# Configuration

Kla searches for a configuration file in the following places

- `config.toml`
- `~/.kla.toml`
- `~/.config/kla/config.toml` _prefered_
- `/etc/kla/config.toml`

The first file that it finds will be read, and used as the main configuration file.
